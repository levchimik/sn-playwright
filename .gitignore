# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~

# MO2 per-install metadata (not part of the mod itself)
mod/meta.ini

# FOMOD build output (regenerated from mod/ on demand)
dist/

# Scratch / scrap workspace (decompiles, backups, vanilla sources, verify dumps)
_tmp/
