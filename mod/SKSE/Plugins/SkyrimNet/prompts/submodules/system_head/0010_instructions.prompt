## Task
{% set pwExcluded = is_in_faction(player.UUID, "SkyrimNet_ActorBlacklistFaction") %}
{% if render_mode == "transform" %}
{% if length(promptForDialogue) > 0 %}
Rewrite this line in your own voice: "{{ promptForDialogue }}"
Express the same meaning using your character's vocabulary, mannerisms, and emotional state.
{% else %}
Speak naturally as {{ decnpc(npc.UUID).name }} in this moment. React to what's happening around you.
{% endif %}
{% elif render_mode == "thoughts" %}
Think internally as {{ decnpc(npc.UUID).name }}. React to what just happened and what it means to you.
{% elif render_mode == "book" %}
Reflect on what you just read. Summarize key points and consider what they mean for you personally.
{% else %}
Respond as {{ decnpc(npc.UUID).name }} in conversation.{% if responseTarget and responseTarget.UUID != 0 and not (pwExcluded and responseTarget.type == "player") %} You are speaking to {% if responseTarget.type == "player" %}{{ player.name }}{% else if responseTarget.type == "npc" %}{{ decnpc(responseTarget.UUID).name }}{% endif %}.{% endif %}{% if pwExcluded %} {{ player.name }} is absent from the scene — don't address them directly. They may come up if relevant; otherwise speak with the other NPCs present.{% endif %}
{% endif %}
